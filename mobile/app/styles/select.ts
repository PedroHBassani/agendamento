import { StyleSheet } from "react-native";

export default StyleSheet.create({
  inputContainer: {
    marginBottom: 16,
  },
  label: {
    color: "#fff",
    fontSize: 14,
    marginBottom: 4,
  },
  inputWrapper: {
    backgroundColor: "#000",
    height: 40,
    borderColor: "#fff",
    borderWidth: 1,
    borderRadius: 4,
    marginTop: 5,
    justifyContent: "center",
    overflow: "hidden",
  },
  picker: {
    backgroundColor: "#000",
    color: "#fff",
    width: "100%",
    height: "100%",
  },
});
