import { StyleSheet } from "react-native";

export default StyleSheet.create({
  inputContainer: {
    marginBottom: 16,
  },
  label: {
    color: "#fff",
    fontSize: 14,
    marginBottom: 4,
  },
  input: {
    height: 40,
    borderColor: "#fff",
    borderWidth: 1,
    paddingHorizontal: 8,
    color: "#fff",
    borderRadius: 4,
    marginTop: 5,
  },
});
