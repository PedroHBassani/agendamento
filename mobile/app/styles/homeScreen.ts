import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: "center",
  },
  logout: {
    color: "#fff",
    padding: 10,
    borderRadius: 5,
    fontWeight: "bold",
  },
});
